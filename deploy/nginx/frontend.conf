server {
    listen 80;
    listen [::]:80;
    server_name yourdomain.com www.yourdomain.com;

    root /var/www/stock-guard/frontend-main/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
